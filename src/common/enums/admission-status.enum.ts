/**
 * New Admission module statuses (Principal updates)
 */
export enum AdmissionStatus {
  NOT_JOINED = 'NOT_JOINED',
  JOINED = 'JOINED',
}

export enum RewardStatus {
  PENDING_REWARD = 'PENDING_REWARD',
  REWARDED = 'REWARDED',
}
